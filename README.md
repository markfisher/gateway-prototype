launch with the gradle task 'blocking' or 'nonblocking'

the number of messages to be processed can be passed with property 'n'

for example:

    ./gradlew blocking -Pn=10

    ./gradlew nonblocking -Pn=100000
